# Korea Score Predictor 포트폴리오 메모

## 프로젝트 제목

Korea Score Predictor

## 한 줄 설명

매장 고객이 QR 코드로 접속해 대한민국 경기 스코어를 예측하고, 제출 후 집계 결과를 확인하는 모바일 우선 이벤트 앱이다.

## 문제

매장에서 고객이 짧은 시간 안에 참여할 수 있는 이벤트가 필요하다. 동시에 휴대폰 번호를 다루기 때문에 중복 참여 확인과 개인정보 보호를 함께 고려해야 한다.

## 해결

Next.js 기반의 간단한 MVP를 만들고, 공개 이벤트 페이지, 결과 페이지, 보호된 관리자 영역, Supabase 저장소, 휴대폰 번호 해시 기반 중복 확인, 마스킹된 관리자 표시를 제공한다.

## 기술 스택

- Next.js App Router
- TypeScript
- Supabase Postgres
- Tailwind CSS
- shadcn/ui
- Vercel

## 핵심 기능

- QR 코드로 접근하기 좋은 모바일 `/event` 페이지
- 닉네임과 한국 휴대폰 번호 입력
- 경기별 스코어 예측 카드
- 제출 전 개인정보 동의
- 휴대폰 번호 해시 기반 중복 참여 방지
- `/result`에서 집계된 스코어 분포 확인
- 보호된 관리자 대시보드
- 경기 관리와 CSV 내보내기

## 현재 화면 구현 메모

- 첫 화면은 매장 고객이 QR 스캔 직후 바로 입력할 수 있도록 단일 모바일 폼으로 구성했다.
- 결과 비율은 제출 전 노출하지 않는 정책을 UI 구조에도 반영했다.
- 휴대폰 번호 안내 문구는 중복 확인과 해시 처리를 짧게 설명하는 수준으로 유지했다.
- 제출 흐름은 server action으로 구현해 브라우저가 Supabase service role key나 해시 secret을 알 수 없게 했다.
- 결과 화면은 개인정보 없이 전체 참여자 수와 경기별 예측 비율만 보여주는 작은 리포트 형태로 구성했다.
- 관리자 화면은 매장 운영자가 필요한 숫자와 최근 제출 내역을 빠르게 보는 운영형 대시보드로 구성했다.
- 토너먼트 진출 등으로 상대팀이 바뀌는 상황을 고려해 관리자가 경기 카드를 직접 추가/수정할 수 있게 했다.
- 경기 후 실제 결과를 입력하면 관리자 대시보드에서 경기별 당첨 후보를 확인할 수 있게 했다.
- 경기 관리 화면은 경기 수가 늘어도 다루기 쉽도록 추가/관리 탭으로 분리했다.
- 실제 매장 운영에서는 한 경기만 QR 이벤트에 노출되도록, 새 경기를 추가하거나 숨김 경기 표시 시 기존 표시 경기는 자동으로 숨기게 했다.
- 이벤트 화면은 경기 시작 시간과 예측 마감 카운트다운을 강조해 현장 참여자가 남은 시간을 바로 이해할 수 있게 했다.
- CSV export는 마스킹 번호와 예측 정보만 포함하도록 제한했다.

## 개인정보 관련 결정

- 원본 휴대폰 번호는 기본 저장하지 않는다.
- 휴대폰 번호는 정규화 후 서버에서 해시한다.
- `phone_hash`는 HMAC SHA-256으로 생성한다.
- 관리자 화면에는 마스킹된 번호만 표시한다.
- 포트폴리오에는 실제 데이터 대신 목업 또는 마스킹 데이터를 사용한다.

## 아키텍처 요약

공개 앱은 Next.js App Router의 `/event`와 `/result` 흐름을 사용한다. 민감한 검증, 해시 처리, Supabase 쓰기 작업은 서버 사이드에서 수행한다. Supabase Postgres는 이벤트, 경기, 참여자, 예측 데이터를 저장하며, 결과 페이지는 집계 query 또는 view를 통해 예측 비율을 표시한다.

## 데이터 모델 요약

- `events`: QR 이벤트 단위와 중복 참여 기준
- `matches`: 이벤트에 포함된 경기 카드와 예측 가능 상태
- `participants`: 닉네임, 해시된 휴대폰 번호, 마지막 4자리
- `predictions`: 경기별 예상 점수
- `score_distribution`: 예측 결과 비율 계산용 view

## 배운 점

- 실제 운영 가능한 작은 이벤트 MVP를 개인정보 제약과 함께 설계하는 방법
- 공개 UX와 서버 사이드 민감 처리 로직을 분리하는 방법
- 포트폴리오에서 기술 결정과 개인정보 결정을 설명 가능하게 문서화하는 방법

## 실제 운영 후 기록할 지표

- 전체 참여자 수
- 제출 완료율
- 중복 참여 시도 수
- 경기별 가장 많이 예측된 스코어
- QR 흐름에 대한 매장 피드백
- 관리자 확인과 CSV 내보내기에 걸린 운영 시간

## 포트폴리오 캡처 체크리스트

- `/event`는 실제 입력 전 빈 상태 또는 목업 값으로 캡처한다.
- `/result`는 목업 또는 익명 집계 데이터만 사용한다.
- `/admin`은 실제 고객 정보 없이 마스킹된 샘플 데이터로 캡처한다.
- Supabase Dashboard, Vercel 환경 변수, service role key, admin hash는 캡처하지 않는다.
- QR 포스터나 안내 문구에는 `/event` URL만 노출한다.
