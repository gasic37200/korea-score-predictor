# Korea Score Predictor 작업 가이드

## 프로젝트 개요

Korea Score Predictor는 실제 매장에서 사용할 모바일 우선 QR 이벤트 웹앱입니다. 고객은 QR 코드를 스캔한 뒤 닉네임과 휴대폰 번호를 입력하고, 대한민국 월드컵 경기 스코어를 예측합니다. 제출 후에는 전체 참여자 수와 예측 비율을 확인할 수 있습니다.

이 프로젝트는 실제 운영뿐 아니라 포트폴리오에도 사용할 예정이므로, 구현은 단순하고 실용적이며 개인정보 보호 관점에서 설명 가능해야 합니다.

## 기술 스택

- Next.js App Router
- TypeScript
- Supabase Postgres
- Tailwind CSS
- shadcn/ui 예정
- Vercel

## 작업 원칙

- 먼저 단순한 MVP를 만든다.
- 현재 마일스톤 범위를 벗어나는 기능은 추가하지 않는다.
- 불필요하게 복잡한 구조를 만들지 않는다.
- 작업 단위는 작게 유지하고, 각 마일스톤 후 검증한다.
- 민감한 작업은 서버 사이드에서만 처리한다.
- 관리자 비밀번호, Supabase service role key, 해시 secret, 실제 참여자 데이터는 노출하지 않는다.
- 관리자 기능을 수정할 때는 마스킹 표시와 CSV 최소 필드 원칙을 유지한다.

## 문서화 원칙

- 기능을 추가하거나 정책이 바뀌면 관련 문서도 함께 갱신한다.
- `SPEC.md`에는 서비스 요구사항과 정책을 남긴다.
- `TASKS.md`에는 마일스톤 진행 상태를 남긴다.
- `PRIVACY.md`에는 개인정보 처리 기준을 남긴다.
- `PORTFOLIO.md`에는 포트폴리오에 설명할 결정과 배운 점을 남긴다.
- 문서는 기본적으로 한국어로 작성한다.

## 개인정보 보호 규칙

- 휴대폰 번호는 개인정보다.
- 휴대폰 번호는 처리 전에 정규화한다.
- 중복 참여 확인에는 `PHONE_HASH_SECRET`을 사용한 HMAC SHA-256 해시를 사용한다.
- 해시 secret과 암호화 key는 브라우저에 노출하지 않는다.
- 원본 휴대폰 번호는 기본적으로 저장하지 않는다.
- 당첨자 연락 등으로 원본 번호가 꼭 필요하면 암호화해서 저장한다.
- 관리자 화면에는 `010-****-1234` 형태의 마스킹된 번호만 표시한다.
- 포트폴리오 스크린샷에는 실제 데이터 대신 목업, 블러, 마스킹 데이터를 사용한다.

## 검증 명령어

- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run admin:hash`는 관리자 비밀번호 해시 생성이 필요할 때만 사용한다.

## 완료 기준

- 요청된 마일스톤 범위만 구현되어 있다.
- 관련 문서가 함께 갱신되어 있다.
- 검증 명령어가 통과하거나, 막힌 이유가 명확히 기록되어 있다.
- 개인정보 관련 로직은 서버 사이드에서 처리된다.
- 변경 파일, 검증 결과, 가정, 다음 마일스톤이 요약되어 있다.
